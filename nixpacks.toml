# Nixpacks configuration for Railway deployment
# Forces fresh pip install without cache

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
# Use --no-cache-dir and --force-reinstall to bypass Railway's pip cache
cmds = ["pip install --no-cache-dir --force-reinstall -r requirements.txt"]

[start]
cmd = "python server.py"
